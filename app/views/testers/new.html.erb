<h1>New Tester</h1>

<%= form_for @tester, :action => "create" do |form_builder| %>

	<%= render(:partial => "users/form", :locals => { :form_builder => form_builder}) %>

<br />
<div class="actions">
	<%= form_builder.submit "Create Tester"%>
</div>

<% end %>

<%= link_to "BACK", admin_user_managements_path(current_user.id) %>
