
def set_password(u, value)
  u.password = value
  u.password_confirmation = value
end

already_seeded_user = User.find_by_id(1)
p "????????????????????????????????????????????????"
p already_seeded_user.nil?
if already_seeded_user.nil?
	user = User.create do |u|
  	 u.id = "1"
     u.email = "admin@ustr.com"
     u.user_name = "admin@ustr.com"
     u.type = "Admin"
     set_password(u, "password1")  
    end
end
